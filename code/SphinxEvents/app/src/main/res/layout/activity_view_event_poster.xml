<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    tools:context=".ViewEventPosterActivity">

    <ImageView
        android:id="@+id/event_poster_image_view"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        app:layout_constraintDimensionRatio="1:1"
        android:padding="7dp"
        app:srcCompat="@drawable/square_border" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/change_poster_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Change Poster"
            android:textColor="@color/black"
            android:textSize="22sp"
            android:padding="10dp"
            app:backgroundTint="@color/change_poster_button_color"
            app:cornerRadius="4dp"
            android:layout_marginBottom="10dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/back_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Back"
            android:textColor="@color/black"
            android:textSize="22sp"
            android:padding="10dp"
            app:backgroundTint="@color/change_pfp"
            app:cornerRadius="4dp" />

    </LinearLayout>

</LinearLayout>